# 项目基本信息
PROJECT_NAME="Multi-tenant Campus Freshman Registration System Test"
PROJECT_DESCRIPTION="test environment"
VERSION="0.1.0"
API_V1_STR="/api/v1"

# 数据库配置
POSTGRES_SERVER=localhost
POSTGRES_USER=admin
POSTGRES_PASSWORD=123456
POSTGRES_DB=multi_tenant_test
POSTGRES_PORT=5432

# JWT配置
SECRET_KEY=your-secure-aes-key-32-chars-long # 开发环境使用test-secret-key，生产环境使用正式的SECRET_KEY，长度大于32位
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 服务器配置
APP_HOST=0.0.0.0
APP_PORT=8001
DEBUG=true
LOG_LEVEL=DEBUG # 开发环境使用DEBUG，生产环境使用INFO

# CORS配置
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]